function characterPosition(character) {
    // get character's current x and y coordinates
    let currX = character.x;
    let currY = character.y;

    // increase the character's x coordinate by 1 px a cycle
}